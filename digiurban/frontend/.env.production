# Production Environment Variables
# Em produção, a API é acessada via proxy reverso do Nginx
# O Nginx roteia /api para o backend (localhost:3001)

# Usar caminho relativo para produção (Nginx faz o proxy)
NEXT_PUBLIC_API_URL=/api

NODE_ENV=production
